/**var destinos = ["Peru, Bolivia, Ecuador"];
destinos.push("Uruguay");
destinos.unshift("Chile");
console.log (destinos);/** */

var obj = null,
    data = ['platano', 'manzana', 'papaya'];

for(var i = 0; i <= data.length -1; i++) {
    if(data[i] === 'platano') {
        obj = data[i];
    }
}
